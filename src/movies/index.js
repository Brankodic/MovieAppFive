import {MovieDetailsScreen, MovieListScreen} from './screens';
import {HeaderImage, HeaderBackImage} from './components';

const screens = {MovieDetailsScreen, MovieListScreen};

export {HeaderImage, HeaderBackImage, screens};
